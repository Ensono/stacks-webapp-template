name: $(build.SourceBranchName)-init

trigger:
  branches:
    include:
    - "*"
  paths:
    include:
    - deploy/*
    - packages/*
    - build/azDevOps/azure/stacks-template-cli.yml
    exclude: 
    - packages/template-cli/templates/*

pr:
- master

jobs:
  - template: templates/jobs/install-build-test.yml
