import { CreateForm, Layout } from "components"
import React, { FC, useEffect } from "react"

const Create: FC = () => {
    return (
        <Layout>
            <CreateForm />
        </Layout>
    )
}

export default Create